---
/**
 * Protected page example: AppExchange Publishing case study
 * 
 * This page will only be accessible to authenticated users.
 * The middleware checks the protectedRoutes config and redirects
 * unauthenticated users to /login.
 */

import Layout from "../../../../../apps/web/src/layouts/Layout.astro";
import Footer from "../../../../../apps/web/src/components/Footer.astro";

const user = Astro.locals.user;
---

<Layout title="AppExchange Publishing - Protected">
  <main class="container">
    <article>
      <header>
        <span class="badge">Protected Content</span>
        <h1>AppExchange Publishing</h1>
        <p class="subtitle">A complete overhaul of the publishing experience for our trusted partners.</p>
      </header>

      <section class="content">
        <p>
          This is a protected page that requires authentication to view.
          If you can see this content, you have successfully logged in as a guest.
        </p>

        <h2>About this case study</h2>
        <p>
          The AppExchange Publishing experience was buckling under its own weight.
          The rate of Salesforce's acquisitions, each with their own unique marketplaces
          and requirements, meant our customers and partners were struggling with siloed
          and fragmented experiences.
        </p>

        <p>
          As a result of my work on AppExchange Publishing, we were able to:
        </p>

        <ul>
          <li>Centralise the publishing process across multiple tenants</li>
          <li>Reduce the time to publish by 2.5x</li>
          <li>Dramatically speed up the security review process by 3.5x</li>
          <li>Process approvals 65% faster</li>
        </ul>

        <p class="note">
          Replace this content with your actual case study content.
          Images and assets should go in <code>packages/protected/assets/</code>.
        </p>
      </section>
    </article>

    <footer>
      <Footer />
      <p class="back-link">
        <a href="/">← Back to home</a>
      </p>
    </footer>
  </main>
</Layout>

<style>
  .container {
    max-width: 48rem;
    margin: 0 auto;
    padding: 2rem 1rem;
  }

  header {
    margin-bottom: 2rem;
  }

  .badge {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    padding: 0.25rem 0.5rem;
    background: #fef3c7;
    color: #92400e;
    border-radius: 0.25rem;
    margin-bottom: 1rem;
  }

  h1 {
    font-size: 2.5rem;
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 0.5rem;
  }

  .subtitle {
    font-size: 1.25rem;
    color: #6b7280;
  }

  h2 {
    font-size: 1.5rem;
    font-weight: 600;
    margin-top: 2rem;
    margin-bottom: 1rem;
  }

  .content p {
    margin-bottom: 1rem;
    line-height: 1.7;
  }

  .content ul {
    margin: 1rem 0;
    padding-left: 1.5rem;
  }

  .content li {
    margin-bottom: 0.5rem;
    line-height: 1.6;
  }

  .note {
    font-size: 0.875rem;
    padding: 1rem;
    background: #f3f4f6;
    border-radius: 0.5rem;
    color: #4b5563;
  }

  .note code {
    font-family: monospace;
    background: #e5e7eb;
    padding: 0.125rem 0.25rem;
    border-radius: 0.25rem;
  }

  footer {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid #e5e7eb;
  }

  .back-link {
    margin-top: 1rem;
  }

  .back-link a {
    color: #6b7280;
    text-decoration: none;
  }

  .back-link a:hover {
    color: #374151;
    text-decoration: underline;
  }

  @media (prefers-color-scheme: dark) {
    .badge {
      background: #78350f;
      color: #fef3c7;
    }

    .subtitle {
      color: #9ca3af;
    }

    .note {
      background: #1f2937;
      color: #9ca3af;
    }

    .note code {
      background: #374151;
    }

    footer {
      border-color: #374151;
    }

    .back-link a {
      color: #9ca3af;
    }

    .back-link a:hover {
      color: #d1d5db;
    }
  }
</style>
